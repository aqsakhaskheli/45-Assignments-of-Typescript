function makeShirt(size: string, text: string): void {
    console.log(`\n you order a ${size} shirt that say ${text}`)
}

makeShirt(`large`, `"i love typescript"`);
makeShirt(`medium`, `"i need a big shirt"`);
